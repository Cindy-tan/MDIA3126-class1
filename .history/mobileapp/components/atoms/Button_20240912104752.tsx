import { Button, View } from "react-native";

export default function myButton({buttonT}) {
    return (
        <View>
            <Button
                color="red"
                title={buttonTitle}
            />
        </View>
    )
}